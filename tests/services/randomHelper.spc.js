/**
 * Created by Administrator on 14/12/14.
 */

describe("random helper service", function() {
  beforeEach(module('common/services/randomHelperSvc'));
  //Not called, I should learn about testing
   it("should call ", inject(function(randomHelperSvc) {

    expect(true).toEqual(true);
   }));
});
